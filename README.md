# ERI-engine

## Compilation

```bash
meson setup build
ninja -C build
```

and test your compilation via

```bash
meson test -C build
```

or using verbose output

```bash
meson test -C build --verbose
```